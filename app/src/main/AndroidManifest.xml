<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.customerviewdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Light.NoTitleBar">
        <activity android:name=".SlideActivity"></activity>
        <activity android:name=".MyContactsActivity" />
        <activity android:name=".MyRippleActivity" />
        <activity android:name=".MyDialActivity" />
        <activity android:name=".MyViewpagerActivity" />
        <activity android:name=".MyAttributeActivity" />
        <activity android:name=".SwitchActivity" />
        <activity android:name=".PopupWindowActivity" />
        <activity android:name=".ADBarActivity" />
        <activity android:name=".YouKuMenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>